{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": [
      "vitest/globals",      // Avoiding imports of describe, it and expect
      "jest-extended",       // Typing for extra matchers
      "vitest/importMeta",   // Tell TypeScript about import.meta.vitest
    ]
  },
  "include": [".", "../src"]
}
